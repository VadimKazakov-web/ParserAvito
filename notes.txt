команды для создания exe файла

* установить python-manager.msix:
    Add-AppxPackage <path to MSIX>

sudo apt update && sudo apt install python3 && sudo apt install python3-pip python3-dev python3-venv -y; python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt && pyinstaller parser.spec

в папке dist появится exe файл с программой

# os.environ["PYTHONUTF8"] = "1"
# print("PYTHONUTF8 ", os.environ.get("PYTHONUTF8"))
# print("flags.utf8_mode ", sys.flags.utf8_mode)
# print("filesyscodec ", sys.getfilesystemencoding())
# print("locale ", locale.getpreferredencoding())

